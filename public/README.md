# Public

## Purpose

This folder stores static files that are shared across the application.

Subfolders:

- css/
    Shared CSS files.

- js/
    Shared client-side JavaScript files.

- images/
    Shared images, icons and other media assets.

Only place files here if they are intended to be used by multiple features.